package org.yoregs.machine.a

// TYPES
//data class InternalChoice(val id: String)  // ⊕ = plus = additive disjunction
//data class Tensor(val id: String)  // ⊗ = times = multiplicative conjunction
//data class ExternalChoice(val id: String)  // & = with = additive conjunction
//data class Par(val id: String)  // ⅋ = par = multiplicative disjunction
//data class Lollipop(val id: String)  // ⊸ = lolly
//data class Forward(val id: String)  // ↔ = identity
//data class Bang(val id: String)  // ! = of course
//data class WhyNot(val id: String)  // ? = why not
//data class ParallelComposition(val r: SessionRole) // | = cut = parallel composition
