package org.yoregs.machine.a

// TYPES
//data class InternalChoice(val id: String)  // ⊕ = plus
//data class ExternalChoice(val id: String)  // & = with
//data class Tensor(val id: String)  // ⊗ = times
//data class Lollipop(val id: String)  // ⊸ = lolly
//data class Forward(val id: String)  // ↔ = identity
// ⅋ = par = multiplicative disjunction
// ! = ofcourse
//data class ParallelComposition(val r: SessionRole) // | = cut = parallel composition

// TERMS
// case
// . = dot = send
// receive
// continue